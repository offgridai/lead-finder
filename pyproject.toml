[project]
name = "lead-finder"
version = "0.1.0"
description = "AI-assisted acquisition lead sourcing pipeline"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
